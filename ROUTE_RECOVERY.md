# 长断线恢复第二版说明

状态：已完成合成图与假底盘离线实现，并依据 2026-09-15 两轮断线专项实车日志做了定向修复；修复后仍待再次实车验证。公共接口仍是 v0.2，没有增加字段。速度、角速度、云台角度、时间预算和视觉阈值仍是待现场标定的初值。

## 2026-09-16 端点拓扑更新（当前实现，以本节为准）

本轮针对“连通直角被当成断线、重新抓回旧线、看到任意蓝色长条就靠近、无法横移到新线端点”做了结构性修复，但仍只完成离线验证：

- `RouteVision.connected_path()` 对与画面下方相连的蓝线做 Zhang-Suen 骨架化。骨架远端落在画面边界表示路线仍连续（例如连通直角）；远端位于画面内部才是物理断口证据。
- `RouteVision.candidates()` 不再只用整段轮廓的 `fitLine` 两端，而是返回骨架端点、端点向线路内部的切向、是否位于图像内部和分支长度。方块、短毛刺和没有可靠端点的色块不会成为候选。
- 到达旧线断口时把该点设为局部原点并保存旧线切向。搜索阶段把新端点按画面位置和已下发运动做保守的局部位置估计，限制旧/新端点间距，并按世界方向近似值排除与旧线同向的候选；已确认的场地形态是断开的直角，因此只接受与旧线约 `50°～130°` 的新线候选。该位置与方向目前来自有限的图像/运动命令估计，不是真实里程计或相机标定结果。
- 连通急弯进入 `CORNERING`，低速沿仍连通的路径转弯，不抬头、不进入空白跨越。真实内部端点才进入 `END_APPROACH → RAISING_VIEW → BRIDGING/SEARCHING`。
- 新线连续确认后按 `ALIGNING → CENTERING → DOCKING → REACQUIRING` 处理：先只转向；再只横移，把断口端点移到车体中心；最后低速前进并同时小幅横移/纠偏；降回巡线视角后仍须基础 `LineDetector` 连续确认才能交还控制权。
- `route.py`、`route_detector.py` 没有连接 SDK，也没有修改 `FramePacket`、`MotionCommand` 或主程序等公共接口。底盘请求仍由协调器和唯一 `MotionOutput` 下发。

旧章节里关于“横向候选一律拒绝”“先朝轮廓下端前进再对齐”以及 `APPROACHING` 状态的描述已经失效；当前场地的垂直新线恰恰是目标，必须通过端点和旧/新切向配对判断。

## 断线专项实车入口

需要排除其他期末任务的误触发、只验证基础巡线和长断线巡回时，运行：

```powershell
python route_only_main.py
```

也可以在 VS Code 中打开 `route_only_main.py` 后直接点击 Code Runner。启动行必须显示
`[ROUTE-ONLY]` 和 `1 task module(s) registered`；否则不是专项入口。该入口不注册
红绿灯、数字标识、障碍物或两类岔路任务，也不订阅 marker，但仍保留统一底盘/云台
出口、人工急停、视频失效锁停和运行记录。

正常蓝线上按一次 `SPACE` 起步。出现 `>> route 接管` 后，基础巡线显示 `STOPPED`
是正常的控制权交接，此时不要再次按空格；再次按空格会作为人工停止立即取消巡回。
遇到危险时仍应按空格停车。

## 为什么单独增加恢复检测器

基础 `line_detector.py` 要求同一轮廓同时覆盖近、远采样带，这适合连续巡线，却会把只有一端进入画面的断口新线判为无效。第二版没有放宽基础巡线条件，而是在 `route_detector.py` 中复用同一套可配置 HSV 与形态学，分别提供：

- `connected_path()`：提取与画面下方相连的路线骨架，返回远端点、端点切向以及端点是否在画面内部；
- `bottom_line()`：保留为走到旧路线末端时的低视角纠偏观测；
- `candidates()`：在画面约 2%～99% 高度、2%～98% 宽度内寻找细长路线，返回骨架端点、端点切向、边界属性和近/远属性。

恢复检测器拒绝面积过小、过粗、不够细长或没有可靠骨架端点的色块。候选切换有位置/无向角连续性门槛，近/远分类使用进入 0.78、退出 0.70 的滞回，减少逐帧状态跳变。当前已知的新路线接近横向，因此横向本身不是拒绝条件；候选必须通过内部端点、旧/新切向夹角和局部距离配对。它不替代基础检测器，也不连接摄像头或 SDK。

## 状态与动作

```text
MONITORING
  ├─ 基础线有效：不接管
  ├─ 基础远采样消失，但骨架从车前连续延伸到画面边界
  │    → CORNERING（0.07 m/s 沿连通急弯低速转向，不当成断线）
  ├─ 发现画面内部的稳定骨架端点，确认 0.08 s
  │    → END_APPROACH（0.10 m/s 低速到达物理线尾）
  │         └─ 下方线消失后停车确认 0.14 s
  │              → RAISING_VIEW
  └─ 整幅路线持续不可见超过基础 0.28 s 容错 + 0.05 s
       → RAISING_VIEW

RAISING_VIEW
  └─ 底盘停止；云台从 -25° 请求到 -12°；按角度/速度计算等待约 0.55 s
       → BRIDGING（沿旧路线切向 0.18 m/s 跨越空白，至少 0.8 s、最多 3 s）
            ├─ 连续 3 帧内部端点，且通过旧/新端点配对 → ALIGNING
            └─ 空白跨越预算用完 → SEARCHING

SEARCHING
  └─ 底盘零前进，按最后方向依次检查 ±30°、±60°、±95°，
     主搜索 45°/s、接近目标或观察候选时 30°/s，硬限制 ±100°
       └─ 候选连续 3 帧，且通过内部端点、局部距离和约 90° 切向夹角检查
            → ALIGNING

ALIGNING（只按新路线端点切向转正，避免位置项和角度项互相抵消；≤18° 连续 3 帧）
  → CENTERING（前进为零，以最大 0.12 m/s 横移对准断口端点）
  → DOCKING（0.08 m/s 前进，同时保留有限横移和角度纠偏；近端且入口误差 ≤0.13 连续 3 帧）
  → REACQUIRING（停车、降回 -25°，用基础 LineDetector 确认 3 张新帧）
  → COMPLETED

活动状态总计达到 19 s、走到旧线尾超过 5 s 或扇扫耗尽 → FAILED 并停车
```

每次 `step(frame, now)` 只处理一帧并返回一个 `TaskUpdate`，不存在 `sleep` 或阻塞扫描。任务只产生 `MotionCommand`/`GimbalCommand`，底盘和云台仍分别经过 `MotionOutput` 与 `GimbalOutput`。

## 旧路线与新路线的区分

当前实现采用五层保守门槛：

1. 先用画面下方检测走到旧路线物理末端，末端确认后才把局部位移清零；
2. 空白跨越先强制执行至少 `0.8 s` 的旧线清离；期间即使旧路线仍被近全屏检测器看到，也不会打断跨越；
3. 候选必须有位于画面内部的骨架断口端点，不能只是一段轮廓或从画面边缘穿过的旧线；
4. 跨越期间累计已下发运动，并把候选端点与旧端点组成局部几何对；前向位置、端点距离和最小清离距离不合格时拒绝；
5. 把候选端点切向换算到局部世界方向，只有与旧线相差约 `50°～130°` 的稳定候选才会接收，因此重新扫到同向旧线不会触发靠近。

这能降低重新抓住身后旧路线的概率，但当前累计量是根据已下发命令计算的保守估计，不是真实里程计，也没有相机标定后的地面单应变换。因此不能声称已经可靠解决所有“旧线/新线”歧义；现场仍要用真实断口验证，必要时再由负责人决定是否接入底盘姿态/里程反馈。

## 借鉴与取舍

- 本地竞速程序：保留最后方向首扫、左右交替、真实时间预算、多帧确认和限幅；不复用 340°/s 竞速参数或完整搜索状态链。
- 传统巡线与恢复案例：使用低计算量的颜色分割、轮廓几何和闭环小步动作；没有复制第三方项目代码。
- 恢复研究中的局部搜索思路：先沿已知切向有限跨越，再扩大角度搜索，并用连续观测降低误抓；不引入 YOLO、视觉里程计、地图或路径规划框架。
- RoboMaster SDK：保持 `CHASSIS_LEAD`，用底盘 yaw 带动水平视野，只通过公共云台出口改变 pitch；任务模块不调用 SDK。

## 离线验证

单模块：

```powershell
python scripts/check_module.py route
```

完整回归：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\check_offline.ps1
```

专测覆盖：正常线不接管、连通直角不触发断线、内部端点确认、下方旧线续行至线尾、抬头期间停车、近全屏骨架端点检测、方形同色块拒绝、空白有限跨越、±95° 扇扫与 ±100° 硬限制、候选三帧确认、旧线方向排除、垂直新线接受、转向/横移/靠近/交接、总超时、人工停止、视频失效和状态清理。

## 必须实车验证

- 抬头后的近全屏 ROI 是否覆盖真实新路线，同时会不会纳入车体或场地外同色物；
- `pitch=-12°` 是否兼顾前方搜索范围和足够地面占比，以及恢复到 `-25°` 后的实际视野；SDK 动作目前仍以角度、配置速度和余量计算等待，并未直接用姿态反馈闭环；
- 空白跨越 `0.18 m/s`、连通急弯 `0.07 m/s`、最终靠线 `0.08 m/s`、最大横移 `0.12 m/s`、搜索 `45°/s` 以及 ±95° 目标是否适合现场距离和相机帧率；
- 底盘 yaw 正负、地面打滑、命令积分误差，以及有夹角/横向错位/弯曲的新路线；
- 旧路线、新路线同时入镜，附近蓝色道具、岔路和其他模块同时触发时的误识别与优先级；
- 找不到线、视频/Wi-Fi 失效、人工停止和程序退出时，实车是否都可靠停车。

## 2026-09-15 实车日志结论

- 原 `pitch=-5°` 的截图包含大量墙面和天花板，路线被压缩到画面底部，因此改为 `-12°`；新角度尚未实车确认。
- 一轮恢复在 `ALIGNING` 中持续约 7.8 秒：候选中心在左侧、角度约 +26.5°，原位置项与角度项几乎完全抵消，最终 yaw 仅约 +0.5°/s。另一轮在线右侧、角度约 -59° 时发生同类抵消。现将“线路转正”和“靠近入口”拆成两个阶段。
- 原算法近线后只原地旋转，无法可靠消除横向错位。当前实现已改为骨架断口端点，并增加独立 `CENTERING` 横移阶段；`DOCKING` 只在转向和横移对准后前进。
- 原 `REACQUIRING` 只确认高视角候选。现必须先降回巡线视角，再由基础检测器连续确认；失败会明确停车，不把“看到候选”写成恢复成功。
