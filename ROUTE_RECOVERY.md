# 长断线恢复第二版说明

状态：已完成合成图与假底盘离线实现，未连接或运行实车。公共接口仍是 v0.2，没有增加字段。速度、角速度、云台角度、时间预算和视觉阈值均是待现场标定的初值。

## 为什么单独增加恢复检测器

基础 `line_detector.py` 要求同一轮廓同时覆盖近、远采样带，这适合连续巡线，却会把只有一端进入画面的断口新线判为无效。第二版没有放宽基础巡线条件，而是在 `route_detector.py` 中复用同一套可配置 HSV 与形态学，分别提供：

- `bottom_line()`：判断旧路线是否仍存在于画面下方，用于低速走到物理线尾；
- `candidates()`：在画面约 2%～99% 高度、2%～98% 宽度内寻找细长的单端线段，返回中心、包围框、方向和近/远属性。

恢复检测器拒绝面积过小、过粗或不够细长的色块；它不替代基础检测器，也不连接摄像头或 SDK。

## 状态与动作

```text
MONITORING
  ├─ 基础线有效：不接管
  ├─ 远采样消失、下方仍有旧线，确认 0.08 s
  │    → END_APPROACH（0.08 m/s，按下方线有限纠偏）
  │         └─ 下方线消失后停车确认 0.14 s
  │              → RAISING_VIEW
  └─ 整幅路线持续不可见超过基础 0.28 s 容错 + 0.05 s
       → RAISING_VIEW

RAISING_VIEW
  └─ 底盘停止；云台从 -25° 请求到 -5°；按角度/速度计算等待约 0.79 s
       → BRIDGING（沿旧路线切向 0.12 m/s 跨越空白，最多 3 s）
            ├─ 连续 3 帧远端候选 → APPROACHING（0.08 m/s，中心和方向共同纠偏）
            ├─ 连续 3 帧近端候选且通过旧线排除 → ALIGNING
            └─ 空白跨越预算用完 → SEARCHING

SEARCHING
  └─ 底盘零前进，按最后方向依次检查 ±30°、±60°、±95°，
     主搜索 30°/s、接近目标或观察候选时 14°/s，硬限制 ±100°
       └─ 候选连续 3 帧并通过离开旧线位置的检查
            → APPROACHING / ALIGNING

ALIGNING（中心误差 ≤ 0.13、线方向 ≤ 18°，连续 3 帧）
  → REACQUIRING（停车再确认 3 张新帧）
  → COMPLETED

活动状态总计达到 19 s、走到旧线尾超过 5 s 或扇扫耗尽 → FAILED 并停车
```

每次 `step(frame, now)` 只处理一帧并返回一个 `TaskUpdate`，不存在 `sleep` 或阻塞扫描。任务只产生 `MotionCommand`/`GimbalCommand`，底盘和云台仍分别经过 `MotionOutput` 与 `GimbalOutput`。

## 旧路线与新路线的区分

第二版采用三层保守门槛：

1. 先用画面下方检测走到旧路线物理末端，末端确认后才把局部位移清零；
2. 空白跨越期间累计已下发运动，近端候选在离开旧线不足 `0.07 m` 时不会被接收；
3. 大角度转向时，突然出现但从未在远处连续观察过的近端线不会直接触发交接。

这能降低重新抓住身后旧路线的概率，但当前累计量是根据已下发命令计算的保守估计，不是真实里程计，也没有相机标定后的地面单应变换。因此不能声称已经可靠解决所有“旧线/新线”歧义；现场仍要用真实断口验证，必要时再由负责人决定是否接入底盘姿态/里程反馈。

## 借鉴与取舍

- 本地竞速程序：保留最后方向首扫、左右交替、真实时间预算、多帧确认和限幅；不复用 340°/s 竞速参数或完整搜索状态链。
- 传统巡线与恢复案例：使用低计算量的颜色分割、轮廓几何和闭环小步动作；没有复制第三方项目代码。
- 恢复研究中的局部搜索思路：先沿已知切向有限跨越，再扩大角度搜索，并用连续观测降低误抓；不引入 YOLO、视觉里程计、地图或路径规划框架。
- RoboMaster SDK：保持 `CHASSIS_LEAD`，用底盘 yaw 带动水平视野，只通过公共云台出口改变 pitch；任务模块不调用 SDK。

## 离线验证

单模块：

```powershell
python scripts/check_module.py route
```

完整回归：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\check_offline.ps1
```

专测覆盖：正常线不接管、下方旧线续行至线尾、抬头期间停车、近全屏远/近/斜线检测、方形同色块拒绝、空白有限跨越、±95° 扇扫与 ±100° 硬限制、候选三帧确认、旧线近端排除、靠近/对齐/交接、总超时、人工停止、视频失效和状态清理。

## 必须实车验证

- 抬头后的近全屏 ROI 是否覆盖真实新路线，同时会不会纳入车体或场地外同色物；
- `pitch=-5°`、抬头等待时间和恢复到 `-25°` 后的实际视野；SDK 动作目前仍以角度、配置速度和余量计算等待，并未读取动作完成反馈；
- 空白跨越 `0.12 m/s`、靠近 `0.08 m/s`、搜索 `30°/s` 以及 ±95° 目标是否适合现场距离和相机帧率；
- 底盘 yaw 正负、地面打滑、命令积分误差，以及有夹角/横向错位/弯曲的新路线；
- 旧路线、新路线同时入镜，附近蓝色道具、岔路和其他模块同时触发时的误识别与优先级；
- 找不到线、视频/Wi-Fi 失效、人工停止和程序退出时，实车是否都可靠停车。
