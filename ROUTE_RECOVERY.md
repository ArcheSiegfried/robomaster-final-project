# 长断线恢复第一版说明

状态：仅完成离线实现，未连接或运行实车。搜索俯角、速度、角速度、时间和视觉阈值都需要按实际场地标定。

## 状态与边界

```text
MONITORING
  └─ 连续丢线 > lost_grace_seconds + 0.05s
       → RAISING_VIEW（底盘停止，云台由 -25° 请求到 -5°）
          ├─ 远端线段连续确认 → BRIDGING（0.07m/s，有限转向）
          └─ 无路线 → SEARCHING（零前进，45deg/s 左右扩大扫描）
               └─ 近端路线 → REACQUIRING（停车连续确认3帧）
                    ├─ 稳定 → COMPLETED
                    └─ 丢失/跳变 → SEARCHING

任何活动状态总计达到5s → FAILED
```

基础巡线的0.28秒普通漏检不属于本模块，不能通过延长它实现长断线。视频失效和人工停止由协调器立即终止接管。完全看不到路线时，前进与横移始终为零；只有连续两帧看到可信远端线段，才允许极低速靠近。

## 借鉴与取舍

- 本地竞速程序：保留最后方向首扫、左右交替、真实时间预算和多帧稳定重获；不复用340deg/s竞速参数和复杂竞速状态机。
- Overengineering² RoboCup：借鉴从远端线段中心判断断口后的路线；没有复制其GPL代码、阻塞循环、GPIO或陀螺仪架构。
- Self-Healing Visual Recovery：借鉴先做局部有限搜索和多帧确认；不引入视觉里程计、YOLO或地图。
- RoboMaster SDK：保持 `CHASSIS_LEAD`，利用底盘yaw带动相机水平搜索，只动态调整云台pitch；不在任务模块调用SDK。

## 当前场地假设

- 路线颜色使用 `config.VisionConfig` 的HSV范围，默认仍为蓝线。
- 能看到断口后线段时，它位于画面中部、不是靠近边缘的大面积同色道具。
- 不知道断线真实长度、错位和附近岔路，因此跨越阶段速度与时间采取保守上限；不满足假设时会转为原地搜索或失败停车。
- 障碍模块目前排在 RouteTask 之后；组合场景的优先级需要整合负责人实车前复核。

## 离线验证

单模块：

```powershell
python scripts/check_module.py route
```

完整回归：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\check_offline.ps1
```

覆盖普通路线不接管、短时漏检不接管、抬头时停车、完全丢线原地交替扫描、远端线段连续确认后低速靠近、三帧稳定重获、超时失败、人工停止、视频失效和巡线视角恢复。

## 尚未证明

- `pitch=-5°`能覆盖实际断线远端，且恢复到`-25°`后相机安装误差可接受。
- 真实底盘yaw正负、转速、地面打滑和线段重获成功率。
- 实际光照、反光、其他蓝色道具、弯曲断口、错位断口及岔路附近误识别。
- SDK异步云台动作在当前固件上的实际完成时间；代码仅以固定等待时间保守处理。
