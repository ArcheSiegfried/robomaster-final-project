# 障碍检测与绕行说明（`obstacle.py`）

负责人：高睿盈（4 号 / WP5）。对应接口版本 `MODULE_GUIDE.md` v0.1。

状态：**已完成离线实现与离线验证，未再次上真车。** ROI 位置、起手时序、动作幅度、
颜色判据这几组参数都**没有实车依据**，需要按实际场地与道具标定；`MotionCommand.lateral`
的正负号必须在**架空**状态下确认。

---

## v5：实车误触发（2026-09-15 16:15 那次运行）

现象：**模块在正常巡线中反复触发**，26 张现场截图里 **11 张被判为"有障碍"（42%）**，
车于是反复"左移 → 回正"。那次运行的两次接管各 **5.2 秒**，
说明 v4 的分段推进已经修好（动作序列真的完整跑完了），但**误触发本身还在**。

报告给的线索与对应的三道闸：

| 线索 | 闸门 |
| --- | --- |
| 11 个框里 **9 个压在 ROI 的下/左/右沿**上。真障碍是"挡在路中间"的，不该被 ROI 切开；被切开的更像背景、场地边界或车体自身 | **闸一 贴边淘汰**（`REJECT_EDGE_TOUCHING`）。**上沿不算**——远处的东西本来就从上面露出来 |
| 误判 confidence 分布 0.38~0.85，而原门槛 0.35 —— 形同虚设 | **闸二** `MIN_CONFIDENCE` 0.35 → **0.50** |
| 关掉颜色判据后只剩"硬边 + 连成块"，对**高对比背景**没有区分力 | **闸三** `MIN_OBJECT_RATIO`：框内"非浅色地面、非蓝线"的像素要 ≥5%。背景/场地边界内部几乎全是地面色，会在这里被拒 |

**门槛为什么取 0.50 而不是报告建议的 0.60**：那批误判里"没贴边"的只有两帧，
置信度 0.39 和 0.61；门槛放在 0.50 正好吃掉 0.39 那帧，又不像 0.60 那样
会误伤"真实但稍远、稍小"的障碍（那种打分本来就在 0.6 附近）。

**用报告记录的 11 个框回放**（框尺寸和置信度是当时用本模块检测器量出来的，可以直接复用）：

| 结果 | 数量 |
| --- | --- |
| 原有命中 | 11 |
| 被闸一（贴边）干掉 | **9** |
| 被闸二（置信度）干掉 | **1** |
| 剩下 | **1**（t=22.08s，conf 0.61，106×60，不贴边） |

剩下那个框需要 HSV 才能判断闸三是否命中，报告里没有给，
**请用那 26 张截图离线重跑探针确认**（目标 11 → 0）。

⚠️ 已知副作用：形态学闭运算会把外接框向外扩约 8 px，
所以**离 ROI 侧沿 8 px 以内的障碍也会被判成"贴边"**。
考虑到官方信息是"障碍停在线中间"，这个代价可以接受，但需要实车确认。



## v4：从实车运行记录里查出来的两个 bug（2026-09-15 报告）

实车测试报告给了 4 次运行的逐次状态时间线。**报告里关于"成功"的结论需要更正**，
并顺着它查出一个真实缺陷。

### 一、报告里那 6 次 `COMPLETED` 极可能是假象，不是真的绕过

报告的时间线里有这样的记录：

```text
00:04.9  TASK_ACTIVE   obstacle  RUNNING    task took over
00:04.9  RELEASING     obstacle  COMPLETED  task completed
```

**整个绕行动作最短也要 4.5 秒**（侧移 1.7 + 前进 1.6 + 回收 1.2，再加找线），
**0.1 秒就 COMPLETED 在物理上不可能**。那次 `FAILED` 同样是接管与失败落在同一瞬间。

原因：**协调器在视频中断 / 人工按键 / 硬超时时会把控制权拿走，但不会通知模块。**
模块的 `stage` 还停在 `OUT`/`PASS`，而每一段的"已用时间"早已超过各自时限，
于是**一帧跳一段**（`OUT→PASS→BACK→SEEK`），看到线就报 `COMPLETED`。

**所以"流程在实车上走通过 6 次"这个结论大概率不成立——绕行动作很可能一次都没真正跑完。**

### 二、第二个 bug（更严重）：`PASS` 和 `BACK` 两段被整段跳过

`_step_active()` 里 `elapsed` 只算了一次、进入下一段时**没有归零**。
侧移段结束的那一刻 `elapsed = 1.7`，它同时 `>= T_PASS_TIME(1.6)` 和 `>= T_BACK_TIME(1.2)`，
于是一次调用里 `OUT→PASS→BACK→SEEK` 全部穿透——**"前进越过"和"回收"两段根本没执行**。

也就是说：即使是一次"正常完成"的绕行，车也只是**横移一下就直接去找线**，
从来没有往前绕过障碍。这个 bug 是靠上一条顺藤摸瓜找出来的。

### 三、两处修法

1. 新增断档检测 `STALE_STEP_GAP = 1.0`：两次 `step()` 之间隔超过 1 秒，
   就认为中途被从外面踢过（实测那次断档 2.6 秒；被踢后巡线是 `STOPPED`，
   要人工按 `SPACE` 才恢复），**作废当前这一轮、重新判断**，绝不从半路接着走。
   同时**不设冷却**——这一轮本来就没绕成，障碍多半还在，应该马上重来。
2. 每进入下一段都把 `elapsed` 归零，**一次调用只推进一段**。

新增三条回归测试钉死它们：`test_restarts_cleanly_after_an_external_release`、
`test_never_completes_before_the_dodge_has_actually_run`（完成时间必须 ≥ 4.5 秒），
以及"整段绕行必须真的发出过前进命令"的断言。

### 四、按官方信息（障碍是静止的小车）修正距离

| 项 | v3 | v4 | 依据 |
| --- | --- | --- | --- |
| `PASS` 前进越过 | 0.20 × 1.60 ≈ 32 cm | **0.20 × 2.20 ≈ 44 cm** | 两车各约 31 cm 长，要完全越过需 ≥41 cm |

并新增三条参数自检测试：距离是否够错开/越过一辆同型车、`MAX_TOTAL_TIME` 是否够走完四段、
自定速度是否都在骨架限幅内。测试总数 35（v5 又加了两条：贴边淘汰、浅色结构）。

---

## 官方信息与距离换算

【官方信息】**障碍是一辆静止的小车**（另一台 RoboMaster 同型车），不会移动。

按"同型车"估算：长约 30~32 cm、宽约 24 cm、高约 27 cm。三个动作距离就是照它算的：

| 段 | 距离 | 怎么来的 |
| --- | --- | --- |
| `OUT` 侧移让开 | 0.24 × 1.70 ≈ **41 cm** | (24 + 24) / 2 + 余量 10 —— 把两辆车的**宽度**完全错开 |
| `PASS` 前进越过 | 0.20 × 2.20 ≈ **44 cm** | (30 + 32) / 2 + 余量 10 —— 把两辆车的**长度**完全越过 |
| `BACK` 往回收 | 0.20 × 1.20 ≈ **24 cm** | 收回让开量的一部分，余下交给 `SEEK` 边挪边找线 |

> v3 的 `PASS` 只有 0.20 × 1.60 ≈ 32 cm，**比需要的 41 cm 短**——
> 那样车会"横移让开了、往前只开一半、然后又收回来"，等于没绕过去。v4 已加长到 44 cm。

障碍是**静止**的，所以它不会自己让开、也不会追上来；但反过来说，
**"绕完还看到同一个障碍"就说明没绕过去**，这时连绕上限会停下来交给人看。

## 状态与边界

```text
IDLE
  └─ 连续 CONFIRM_FRAMES 帧检出障碍
       → OUT（立刻侧移让开，0.24 m/s × 1.70s ≈ 41cm）
          → PASS（前进越过，0.20 m/s × 2.20s ≈ 44cm）
             → BACK（侧移回中线，0.20 m/s × 1.20s ≈ 24cm）
                → SEEK（一边往回挪一边看蓝线，最长 1.50s）
                     ├─ 连续 2 帧看到蓝线 → COMPLETED
                     └─ 超时没看到      → FAILED（停车）

任何阶段总计达到 9s          → FAILED（停车）
同一段路连绕满 2 次障碍还在   → 接管一帧后直接 FAILED，并**上锁**不再接管
                                （障碍消失 CLEAR_SECONDS=3s 才解锁）
被从外面踢掉（断档 > 1s）      → 作废当前这一轮，重新判断（见 v4 一节）
```

- **默认没有停车段**：`HOLD_BEFORE_GO = 0`，确认完成的那一帧就已经在下发侧移。
- 一旦返回 `RUNNING`，后续必须持续 `RUNNING`，直到 `COMPLETED` 或 `FAILED`。
- 视频中断、人工 `SPACE` / `R`、`nan/inf`、命令限幅、20s 硬超时**全部由协调器负责**，
  本模块不重复实现，也不接触唯一运动出口。

## 检测判据

**默认只用"结构"判据，不用颜色。** 理由见下一节。

```text
ROI (0.25, 0.30, 0.75, 0.75)  ← 靠上、靠窄：车体自己落在 ROI 外面，障碍也能更早发现
  └─ 把蓝线像素在灰度图上抹成地面色（线本身不产生边缘，也不切断障碍轮廓）
       → Canny(40,110) → dilate(3) 加粗 → close(17) 连成块
          → 外接框判据：面积 3%~60%、宽 10%~60%、高 ≥22%、框内密度 ≥5%
             → 闸一 贴边淘汰：压住 ROI 下/左/右沿的直接丢（上沿不算）
                → 闸三 框内必须真的有东西：非浅色地面、非蓝线的像素 ≥5%
                   → 皮肤色剔除：候选里低饱和 H 0~25 的像素过半 → 扔掉
                      → 闸二 置信度 ≥0.50
                         → 多个候选按 面积/靠下/居中/宽度 打分，取最高分
```

> 闸一、闸二、闸三是 v5 新加的，专门治实车上的误触发，见下面 v5 一节。

颜色判据（`OBSTACLE_HSV_RANGES`）保留但**默认关闭**（`USE_COLOR_RANGES = False`）。

## 为什么不用颜色：三次实车反馈的来龙去脉

| 版本 | 事件 | 结论 |
| --- | --- | --- |
| v1 | 审核报告 `高睿盈_v1.md`：暗色 HSV 区间 `(0,0,0)~(180,255,70)` 实测把亮度 ≤70 的灰块 100% 判成障碍 | 关掉暗色档 |
| v2 | 审核报告 `高睿盈_v2.md`：同一障碍反复触发（30 秒 7 次） | 冷却 1.5→5s，连绕上限 2 次，锁停 |
| **v3** | 实车测试报告 `高睿盈_v3.md`：**426 张现场画面像素级测量** | 见下 |

v3 报告的三条根因（**不是"检测不到"，是"检测到了也没用"**）：

1. **【P0】相机自己的橙色件常驻在检测区里。** 旧 ROI 是 `(0.20, 0.45, 0.80, 0.95)`，
   而镜头正下方中间恰好是机器人自己的橙色件：426/426 帧都在 ROI 里有橙色像素，
   每帧 900~1700 px，最大色块 73×19。而皮肤 `H 5~25` 与橙色完全重叠 ——
   426 帧里被判为"有障碍"的 4 帧，**全是人的手和手臂**（confidence 最高 0.86）。
   **颜色判据在这个场景里没有分离度。**
2. **【P0】从"看到"到"第一脚侧移"要 0.35 秒**，其中 `HOLD` 段还主动下发三轴全零。
   以巡线 0.32 m/s 计，这 0.35 秒车又前冲约 11 cm；再叠上障碍挡线后 0.28 秒的降速滑行，
   合计约 15 cm 是在"第一脚侧移之前"走掉的。操作者看到的就是"车停住了、没绕"。
3. **【P1】检测区下沿离车头太近**（0.95，与巡线 ROI 0.54~0.96 压在同一块地面上），
   障碍贴到车头才进 ROI；而且越近 `area_ratio` 越大，越容易被 `MAX_OBSTACLE_AREA` 拒掉。

v3 的对应修法：**ROI 抬高收窄 + 关掉颜色判据改用结构判据 + 加皮肤剔除 +
去掉停车段（确认当帧即侧移）+ 面积上限只当兜底**。

## 现场怎么调参（不需要连车、不需要相机）

```powershell
python obstacle.py 样图.png 掩膜.png     # 看检测结果和候选掩膜
```

审核人的只读探针 `tools/probe_obstacle_captures.py` **不需要改动**就能复核本模块
（它引用的 `OBSTACLE_ROI / OBSTACLE_HSV_RANGES / _odd_kernel / OPEN_KERNEL /
CLOSE_KERNEL / MIN_OBSTACLE_AREA / MAX_OBSTACLE_AREA / MIN_OBSTACLE_WIDTH /
MIN_CONFIDENCE` 全部保留）：

```powershell
python tools/probe_obstacle_captures.py --captures captures --csv reports/obstacle_v3.csv
```

现场要调的参数，都在 `obstacle.py` 顶部一节：`DODGE_SIDE`、`SIDE_SPEED`、
`T_OUT_TIME / T_PASS_TIME / T_BACK_TIME / SEEK_TIME`、`EDGE_LOW / EDGE_HIGH /
STRUCTURE_CLOSE`、`OBSTACLE_ROI`、以及四个形状判据。

## 借鉴与取舍

- **轮廓筛选框架**借自本地竞速工程 `blue_line_detector.py` 的"多候选打分选最优"，
  但只用它挑块，不复制其跟踪、外推和竞速参数。
- **蓝线颜色**直接取 `config.CONFIG.vision` 的区间（SEEK 段与抹线用），
  避免模块与巡线出现两套标准。
- **没有**引入任何新依赖、没有新开相机、没有绕开 `MotionOutput`。

## 验证结果

```powershell
python scripts/check_module.py obstacle                            -> MODULE_CHECK_OK（35 tests OK）
powershell -ExecutionPolicy Bypass -File scripts/check_offline.ps1 -> OFFLINE_CHECK_OK
```

两处**离线**复核（都不是真实画面回归）：

- 审核人给的 426 帧逐帧统计：旧判据下 426 帧全部产生橙色候选；按 v3 的尺寸判据，
  **423 帧直接清零**，只剩 2 帧（正是报告指认的手）。车自己那块 72×20 的扁条
  **无论落在 ROI 哪个位置都过不了高度关**（新 ROI 高 162px，要求 ≥36px）。
- 用审核人的探针脚本（一行未改）在 25 张自造模拟画面上：
  车自遮挡 20 帧 **0 触发**、手掌 3 帧 **0 触发**、真障碍 2 帧 **2 触发**。

## 未验证事项（不得当作已通过）

1. **v3/v4 一次真车都没试过。** ROI 位置、起手时序、动作幅度、断档阈值全是纸面推导。
   （2026-09-15 那次实车跑的是 v3，而按上面第一节的分析，**那次没有一次真正跑完过绕行动作**。）
2. **真实障碍道具还没拿到**，它在真实光照下的样子完全未知。
3. **426 张现场画面本身没有**（只有逐帧统计 CSV），所以**没有对真实画面重新跑过检测**；
   CSV 里没有色块坐标和 HSV，"新 ROI 是否完全裁掉自遮挡""皮肤剔除是否命中那 2 帧手"
   只能靠推理。
4. **`MotionCommand.lateral` 正负号仍需架空确认**（`MODULE_GUIDE.md` 要求）。
5. **动作幅度（41cm / 4.5s）没有依据**，道具宽度与场地宽度都没量过。
6. 结构判据的阈值没有真实画面依据，现场可能要调。
7. `evidence.py` 的 `log.csv` 缺 `state / owner / task_name / task_status / errors` 列，
   导致"没绕过去"这类问题无法回溯——这是公共文件，建议由整合负责人统一处理。

## 下次上真车请重点看

1. 车会不会**自己开始绕**（P0-1 修没修好）；
2. 确认之后是不是**立刻侧移**（P0-2：还看到"先停一下"就是没修掉）；
3. **侧移方向对不对**（先架空验，符号反了就是往障碍上撞）；
4. **41cm 够不够**（量障碍宽度与场地宽度）；
5. 人工记录时刻（按 `SPACE` / 开始绕 / 停住），与 `log.csv` 的 `elapsed_s` 对齐。
