# 期末候选任务清单

本表用于拆分小任务或安排配对开发，不指定成员，也不要求每个人立即独立完成完整 PR。场地未确认项不得靠猜测写进正式逻辑。

## 数字标识

- 输入：共享 `FramePacket`，正式标识样图或 SDK 识别结果。
- 输出：`VisualDetection(kind="number_marker", target_id="1".."5", center, box, confidence)`；未识别返回 `no_result`。
- 负责文件：建议 `number_marker.py`、`tests/test_number_marker.py`，少量样图放 `tests/data/markers/`。
- 依赖：OpenCV；只有确认官方 marker 可用后才评估 SDK vision 订阅，模块不得自行开相机。
- 最小验收：识别限定 ID 1～5；无目标、误检、多个目标和重复目标行为明确；在规定尺寸/距离样图上输出像素位置；离线测试通过。
- 待确认：标识样式、尺寸、视距、遮挡、最近目标定义、是否必须使用官方 marker。

## 红绿灯

- 输入：共享 `FramePacket` 和灯具样图。
- 输出：`VisualDetection(kind="traffic_light", color="red"|"green", center, box, confidence)`；不确定必须返回无结果，不能默认绿灯。
- 负责文件：建议 `traffic_light.py`、`tests/test_traffic_light.py`、`tests/data/lights/`。
- 依赖：OpenCV HSV/形状处理，颜色参数进入集中配置。
- 最小验收：红、绿、无灯和干扰色样图可区分；连续确认与状态切换策略有时间上限；输出不直接控制底盘。
- 待确认：灯位置、大小、亮度、红灯熄灭或见绿灯才能通行、所需确认时长。

## 绕障

- 输入：共享帧中的障碍检测，以及确认可用后提供的距离信息。
- 输出：非阻塞 `TaskUpdate` 序列和受限 `MotionCommand`；完成或失败时零运动。
- 负责文件：建议检测与动作分开为 `obstacle_detector.py`、`obstacle_task.py`，测试各自对应。
- 依赖：障碍样图；是否使用 ToF 取决于硬件确认。任务通过 `MotionOutput` 外部所有权运动。
- 最小验收：假检测/假时间下逐步输出有限动作；每步立即返回；超时失败并停车；完成后可进入回线检查。
- 待确认：障碍外观、尺寸、通道宽度、传感器安装、停车距离、允许绕行方向和轨迹。

## 长断线与岔路

- 输入：共享帧、底层 `LINE_LOST` 事件、未来灯或机器人检测结果，以及已确认场地几何。
- 输出：任务级 `TaskUpdate`、有限时间的 `MotionCommand` 和最终完成/失败结果。
- 负责文件：建议 `route_task.py`、`tests/test_route_task.py`；不要修改普通漏检预算。
- 依赖：真实断线和岔路样图、任务规则、上层接管入口。
- 最小验收：短时漏检仍完全交给基础巡线；只有明确任务条件才接管；假时间测试中有硬超时、不无限转圈；找到路线后交回并显式恢复。
- 待确认：断线长度与错位、允许搜索范围、岔路几何、绿灯方向映射、拥堵定义和选择时机。

## 截图记录

- 输入：`FramePacket`、任务检测、任务状态和运行 ID。
- 输出：一次性图片路径及轻量事件记录；不输出运动请求。
- 负责文件：建议 `evidence.py`、`tests/test_evidence.py`。
- 依赖：OpenCV 写图、确定的命名规则和输出目录；写盘不能阻塞控制周期。
- 最小验收：同一任务成功只保存一次；文件名可追踪类别/ID/时间；失败写盘不影响安全停车；输出目录被 Git 忽略。
- 待确认：截图文字、队号、分辨率、提交格式、保存时机和是否允许后台写盘。

## 总体拼合（项目维护者负责）

- 输入：各模块的 `VisualDetection` / `TaskUpdate`、基础巡线状态和共享 `FramePacket`。
- 输出：每周期唯一控制所有者、唯一最终 `MotionCommand`、任务状态显示及安全停车。
- 负责文件：由项目维护者统一修改 `main.py`、`models.py`、`runtime.py`、`motion_output.py` 和集成测试；组员不要各自改一套主流程。
- 依赖：已验收模块、明确任务优先级和期末规则。
- 最小验收：每周期最多一个运动来源；暂停/接管/完成/失败/恢复顺序可离线复现；异常、视频丢失和退出始终停车；完整回归通过。
- 待确认：任务先后顺序、冲突优先级、计分要求、人工干预边界和整场超时。
