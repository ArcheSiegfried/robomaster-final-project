# 可领取模块工作包

本文件只定义工作边界，不指定成员。优先级含义：P0 是本周基础交付或安全前置，P1 是规则明确后争取完成，P2 是时间不足时可降级。所有业务分支从 `integration` 创建，PR 目标也是 `integration`。

公共高冲突文件：`models.py`、`camera_source.py`、`runtime.py`、`motion_output.py`、`main.py`。普通模块开发者不得自行修改；确需变更时先向整合负责人说明接口缺口。

## WP1 基础巡线修复与实车参数

- 目标：证明基础巡线和全部停车路径在实机上的最低可用性，留下可复现参数，不扩张算法。
- 优先级：**P0**。
- 输入：共享 `FramePacket`、`LineDetection`、现有 `config.py`、真实路线和相机安装信息。
- 输出：受限 `MotionCommand`、参数差异、失败案例和实车记录。
- 负责文件：优先 `config.py`、`tests/test_offline.py`、验证记录；有证据时可改 `line_detector.py`、`controller.py`。
- 允许修改：阈值、ROI、控制参数和对应测试；每次只改少量参数。
- 禁止自行修改：全部公共高冲突文件。
- 依赖项：无；它是所有带运动任务的前置。
- 最小验收：离线回归通过；架空确认前进/横移/yaw 符号；低速完成直线、弯道、短遮挡、持续丢线、视频中断和人工停止；记录 commit SHA 与配置。
- 离线测试：运行 `scripts/check_offline.ps1`，补充合成线、干扰色块、假时间、限幅断言。
- 实车测试：必须由负责人安排安全区、急停人员和分阶段用例；不得由代码助手运行。
- 必须交回：源码/配置差异、命令与结果、路线/光照/安装信息、失败录像或帧、未验证项。
- 尚待确认：路线颜色、宽度、材质、弯道、反光、相机位姿、允许速度。
- 降级方案：降低固定速度和转向上限；缩小场景范围；若视频或停车路径不可靠，停止后续运动任务。

## WP2 数字标识识别、筛选与居中

- 目标：识别限定数字 1～5，输出候选位置/ID/置信度，并用非阻塞步骤产生有限居中请求。
- 优先级：**P0**，但样式未知时先做数据集和接口测试。
- 输入：同一 `FramePacket`、正式标识样图/尺寸/距离、任务上下文。
- 输出：`VisualDetection(kind="number_marker", target_id, center, box, confidence)`；任务阶段返回 `TaskUpdate` 与可选 `MotionCommand`。
- 负责文件：建议新增 `number_marker.py`、`number_task.py`、对应测试和少量 `tests/data/markers/` 样图。
- 允许修改：本模块、模块配置和测试；不自行开相机。
- 禁止自行修改：公共高冲突文件。
- 依赖项：接口只依赖 v0.1；运动接入依赖 WP1，总体流程依赖 WP7。
- 最小验收：1～5、无目标、非目标、多目标、重复目标行为明确；每次调用立即返回；居中方向/限幅可由假数据断言；完成或失败归还控制权。
- 离线测试：真实去隐私图片加负样例；构造 `FramePacket`；统计各类误检/漏检，不能只展示成功图。
- 实车测试：测不同距离、角度、光照和运动模糊；记录停车/居中误差。
- 必须交回：源码、测试、可公开样图与来源、准确率统计、阈值、失败样例、未验证项。
- 尚待确认：字体/颜色/尺寸/摆放、视距、遮挡、去重规则、截图时机、是否兼容官方 marker。
- 降级方案：只在固定 ROI 和固定距离识别；取消自动居中改为停车截图；只保留可靠数字集合并明确报告。

## WP3 红绿灯识别与停车/放行

- 目标：可靠区分红、绿、无结果；红灯保持停车，满足连续确认的绿灯才完成放行。
- 优先级：**P0**，规则/灯具未知时先收集样本。
- 输入：共享 `FramePacket`、灯具样图与位置规则、单调时间。
- 输出：`VisualDetection(kind="traffic_light", color="red"|"green", ...)` 和非阻塞 `TaskUpdate`。
- 负责文件：建议新增 `traffic_light.py`、`traffic_light_task.py`、对应测试及少量样图。
- 允许修改：本模块阈值、连续确认状态和测试。
- 禁止自行修改：公共高冲突文件；不得默认“没看到红灯就是绿灯”。
- 依赖项：v0.1；停车/恢复行为参考 WP1；接入依赖 WP7。
- 最小验收：红/绿/无灯/反光干扰均有测试；单帧抖动不放行；红灯输出零运动；完成/失败状态明确且无阻塞等待。
- 离线测试：图片/短回放与假时钟测试颜色、迟滞、超时和状态转换。
- 实车测试：在实际亮度、距离和视角下测误判；验证停车后不会因重新见线自动启动。
- 必须交回：源码、正负样例、状态测试、误判统计、参数、实际结果和未验证项。
- 尚待确认：灯大小/位置/亮度、切换方式、见绿或红灭的放行规则、确认时长。
- 降级方案：只在确认区域工作；提高连续确认并保持保守停车；无法可靠识别时不自动放行。

## WP4 截图与运行记录

- 目标：生成可追溯且不阻塞控制循环的任务证据，避免把运行产物提交进 Git。
- 优先级：**P1**。
- 输入：`FramePacket`、`VisualDetection`、`TaskUpdate`、运行 ID 和事件时间。
- 输出：标注图片相对路径与轻量事件记录；不输出运动。
- 负责文件：建议新增 `evidence.py`、`tests/test_evidence.py`；运行文件写入被忽略的 `captures/`。
- 允许修改：本模块、格式说明、临时目录测试和 `.gitignore` 的必要补充。
- 禁止自行修改：公共高冲突文件。
- 依赖项：可先独立开发；正式字段/触发依赖 WP2、WP3，总体接入依赖 WP7。
- 最小验收：一次任务成功只记录一次；命名可追踪；写盘失败可报告且不影响安全停车；路径不含个人绝对目录。
- 离线测试：临时目录、假帧、重复事件、写入失败和清理边界。
- 实车测试：确认保存延迟不影响控制帧率，检查完整流程截图可读。
- 必须交回：源码、测试、少量脱敏样例、格式/容量说明、失败策略、未验证项。
- 尚待确认：学校实际要求的截图、文字、队号、分辨率、命名和保留数量。
- 降级方案：只保存任务完成关键帧和 CSV/JSONL 摘要；必要时人工整理，不引入复杂后台系统。

## WP5 障碍检测与绕行

- 目标：检测正式障碍，以有硬上限的非阻塞动作绕行，并在结束后重新确认路线。
- 优先级：**P1**；场地和距离来源不明时不得定轨迹。
- 输入：共享帧、障碍样式/尺寸、确认可用的距离信息、时间和路线检测。
- 输出：`VisualDetection`、`TaskUpdate`、受限 `MotionCommand`；失败时停车。
- 负责文件：建议 `obstacle_detector.py`、`obstacle_task.py` 及各自测试。
- 允许修改：模块状态、动作参数、测试夹具；每一步快速返回。
- 禁止自行修改：公共高冲突文件；不得直接调用 SDK 或无限动作。
- 依赖项：WP1；场地/传感器确认；接入依赖 WP7。
- 最小验收：有/无/误检测试；假时钟下每段动作有速度与总时长上限；随时可取消；完成后清巡线历史并要求新鲜路线。
- 离线测试：假检测序列、假时钟、动作限幅、超时/失败/恢复状态。
- 实车测试：从静止和极低速分段验证，设置物理安全区；记录最小间距和失败轨迹。
- 必须交回：源码、测试数据、动作时序、场地假设、超时策略、实车记录、失败案例。
- 尚待确认：障碍外观/位置、通道宽度、传感器、底盘尺寸、安全距离和允许绕行侧。
- 降级方案：检测后停车等待人工；采用单侧固定低速绕行；若安全边界不明确则取消自动绕行。

## WP6 长断线与两类岔路

- 目标：分别处理期末长断线巡回、绿灯岔路和无拥堵岔路；不篡改基础短时漏检语义。
- 优先级：**P1**，高度依赖场地；规则迟迟不明时降为 P2。
- 输入：共享 `FramePacket`、`LineDetection`、底层 `LINE_LOST`、灯/机器人检测和已确认场地规则。
- 输出：有触发条件、硬超时和失败停车的 `TaskUpdate` / `MotionCommand`。
- 负责文件：建议 `route_task.py`、`junction_detector.py` 及对应测试；优先先写一个明确场景。
- 允许修改：外部任务模块、场景配置、假数据/回放测试。
- 禁止自行修改：公共高冲突文件；不得延长 `lost_grace_seconds` 冒充长断线，不得无限扫描。
- 依赖项：WP1；绿灯岔路依赖 WP3；拥堵岔路依赖已确认的检测来源；接入依赖 WP7。
- 最小验收：三类场景各自触发/完成/失败可说明；假时钟下动作有限；任务前暂停巡线，结束后零运动、清历史、新帧确认和显式恢复。
- 离线测试：正式场地录像优先；否则用假检测序列验证状态、方向、超时和错误分支。
- 实车测试：按断线、单岔路、组合场景逐级低速验证，记录几何、方向和重获成功率。
- 必须交回：源码、简短状态图、样本/回放、场地假设、测试结果、失败案例和未验证项。
- 尚待确认：断线长度/错位、巡回标记、岔路几何、绿灯映射、拥堵定义、选择时机。
- 降级方案：只实现已知固定几何；在长断线处停车等待人工；放弃无可靠检测依据的岔路自动选择。

## WP7 总体拼合与完整流程

- 目标：由整合负责人把已验收模块接入唯一主流程，维护控制权、任务顺序、安全停车和回归结果。
- 优先级：**P0**。
- 输入：WP1～WP6 可合并成果、共享 `FramePacket`、任务规则、证据要求。
- 输出：每周期唯一 owner、唯一最终 `MotionCommand`、完整离线流程、分阶段实车记录和稳定标签。
- 负责文件：整合负责人协调 `main.py`、`runtime.py`、`models.py`、`motion_output.py`、集成测试和说明。
- 允许修改：经评审的集成代码；公共接口变化必须说明原因、影响并更新版本。
- 禁止自行修改：普通模块开发者不得各自改主流程或新增 SDK 出口。
- 依赖项：先用假模块验证框架；正式完成度取决于 WP1～WP6 与规则。
- 最小验收：正常巡线、每类接管、完成恢复、任务失败、持续丢线、视频失效、人工暂停、异常退出均离线回归；实车结果与提交 SHA 一一对应。
- 离线测试：统一脚本、模块测试、假任务端到端回放、SDK 调用静态审计。
- 实车测试：按“架空 → 单模块 → 两模块 → 整场”推进；负责人控制版本和急停。
- 必须交回：集成源码、测试结果、最终配置、实车日志/视频索引、已知问题、启动说明和稳定标签。
- 尚待确认：任务顺序、优先级、重试、人工干预、整场超时、计分与提交格式。
- 降级方案：只接入最可靠任务；关闭不稳定模块；保留基础巡线和安全停车作为最低交付。
