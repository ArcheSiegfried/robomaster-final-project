# RoboMaster EP 期末项目规则书

> 适用对象：第 10 组成员、项目负责人、代码整合人员及辅助开发的 Codex/AI Agent
>
> 规则来源：`16-Final_Project_Requirements v8(4).pdf` 第 35-52 页，以及小组收到的红绿灯补充说明。
>
> 本文用于统一理解规则、设计程序状态机、验收各模块和整理最终证据。

## 1. 信息等级

本文将信息分为三类，开发时不得混淆：

1. **课件硬规则**：课件明确写出的任务、分值、照片和扣分要求。
2. **补充规则**：小组另外收到的红绿灯布置说明。
3. **工程建议**：为了让程序稳定完成任务而采用的实现方式，不是课件原文。

如后续教师或助教给出新说明，以最新明确说明为准，并同步更新本文。

---

## 2. 总体任务

机器人必须在无人工操控的情况下沿彩色路线自主行驶，并完成以下任务：

1. 识别数字标识、停车对准并拍照；
2. 在路线断开后自主寻找下一段正确路线并恢复巡线；
3. 检测并绕过道路障碍，随后返回路线；
4. 在红绿灯岔路选择正确道路；
5. 在拥堵岔路选择没有交通拥堵的道路；
6. 在普通路段完成红灯停车、绿灯继续行驶；
7. 为各项计分任务保存符合要求的证据照片；
8. 录制完整运行过程。

比赛过程中每发生一次人工干预，扣 5 分。

---

## 3. 成绩构成

### 3.1 课程总成绩

课件给出的课程总评构成为：

```text
课程总成绩
= 10% × 出勤
+ 40% × Practice（6 次作业）
+ 50% × Final Project
```

### 3.2 每名成员的 Final Project 成绩

```text
Final Project 成绩
= 60% × 团队最终任务成绩
+ 40% × 个人编程成绩
```

同一组不同成员的成绩可以不同。每名成员都必须：

- 参与编程；
- 负责与其他成员不同的程序部分；
- 将自己的代码放在明确的函数或模块中；
- 确保该函数或模块被最终主程序实际调用。

### 3.3 最终任务分值

| 任务 | 分值 |
| --- | ---: |
| 数字标识 1-5 | 每个 5 分，共 25 分 |
| 普通路段红灯停车 | 10 分 |
| 普通路段绿灯继续 | 10 分 |
| 障碍绕行并恢复巡线 | 15 分 |
| 断线后寻找并恢复正确路线 | 10 分 |
| 红绿灯岔路正确选路 | 15 分 |
| 无拥堵岔路正确选路 | 15 分 |
| 合计 | 100 分 |

课件原文写明：“正确选择岔路得 15 分，岔路选择共有两种条件”。课件前文又将“按红绿灯选路”和“按是否拥堵选路”列为两项独立任务。按照这一结构，两类岔路分别按 15 分理解，全部任务合计为 100 分。

---

## 4. 数字标识任务

### 4.1 有效 ID

只有以下五个 ID 参与计分：

```text
1、2、3、4、5
```

规则如下：

- 其他 ID 全部忽略；
- 不要求从 1 开始；
- 不要求按照 `1 → 2 → 3 → 4 → 5` 的顺序；
- 每个有效 ID 独立计 5 分；
- 同一个 ID 成功对准并拍照后，再次出现时必须忽略；
- 例如只完成 `1、2、5`，对应获得 15 分；
- 例如按照 `5、1、2、3、4` 的顺序完成，同样可以获得全部 25 分。

### 4.2 目标选择条件

只有当标识框宽度大于画面宽度的 `1/5` 时，才允许把它作为对准目标：

```text
marker_width > frame_width / 5
```

太远、太小的标识即使被检测到，也不应立即停车拍照。

如果同一画面中同时出现多个数字标识：

1. 排除 ID 不属于 1-5 的标识；
2. 排除本次尝试中已经成功拍摄的 ID；
3. 排除宽度不超过画面宽度 `1/5` 的标识；
4. 在剩余目标中选择距离最近的标识；
5. 单目视觉中可用“检测框宽度最大”近似“距离最近”。

### 4.3 必须完成的动作

```text
继续巡线接近
→ 发现有效且足够大的数字标识
→ 停车
→ 面向标识
→ 将标识对准画面中心
→ 确认对准稳定
→ 保存证据照片
→ 记录该 ID 已完成
→ 恢复巡线
```

只有证据照片保存成功后，才可以把该 ID 记为已经完成。

### 4.4 中心判定

设：

- 画面尺寸为 `W × H`；
- 标识框为 `(x, y, w, h)`；
- 标识中心为 `(x + w/2, y + h/2)`；
- 画面中心为 `(W/2, H/2)`。

课件规定：标识没有位于保存图片中心时扣 2 分，并将允许的中心区域描述为宽、高分别为标识宽、高的 `1/10`。按严格实现可使用：

```python
abs(marker_cx - frame_cx) <= marker_width / 20
abs(marker_cy - frame_cy) <= marker_height / 20
```

连续多帧满足中心条件后再拍照属于工程稳定措施，连续帧数由小组调试确定。

### 4.5 证据照片

数字标识照片必须：

- 保存完整相机画面，不只保存裁剪后的数字；
- 用矩形框标出检测到的标识；
- 显示队伍编号和标识 ID；
- 使用类似文字：

```text
Team 10 detects a marker with ID of 2
```

每个有效 ID 在一次尝试中只保存一张正式计分照片。

---

## 5. 普通路段红灯停车、绿灯继续

该任务是独立计分任务，不是岔路选路任务。

### 5.1 场地和行为

小组需要在自选地点实现：

```text
红灯亮 → 停车并等待
绿灯亮 → 继续行驶
```

分值分别为：

- 成功检测圆形红灯并停车：10 分；
- 成功检测圆形绿灯并继续行驶：另得 10 分。

程序不得把“没有看到红灯”直接视为绿灯。绿灯通行分应基于明确、可靠的绿色圆灯判断。

### 5.2 推荐状态逻辑

```text
正常巡线
→ 连续确认圆形红灯
→ 停车并保持等待
→ 保存红灯停车证据
→ 连续确认圆形绿灯
→ 保存绿灯继续证据
→ 恢复巡线
```

课件明确说明：识别普通红绿灯时不要求转动云台对准。

### 5.3 证据照片

红灯证据：

- 用圆圈标出红灯；
- 在圆圈下方写明检测到红灯并停车；
- 示例：

```text
Team 10 detects a red light and stops the robot
```

绿灯证据：

- 用圆圈标出绿灯；
- 写明检测到绿灯并继续行驶；
- 示例：

```text
Team 10 detects a green light and continues
```

红灯和绿灯是两个独立计分事件，应分别保存照片。

---

## 6. 红绿灯岔路任务

该任务与普通路段“红灯停、绿灯行”分别计分，正确完成岔路选择按 15 分处理。

### 6.1 补充布置规则

小组可以自行选择在岔路使用红灯还是绿灯。总原则是：

> 选择有绿灯的道路，或者避开有红灯的道路。

允许的布置及正确动作如下：

| 岔路灯光布置 | 正确选择 |
| --- | --- |
| 一侧红灯，另一侧绿灯 | 选择绿灯侧 |
| 只在一侧放绿灯 | 选择绿灯侧 |
| 只在一侧放红灯 | 选择没有红灯的另一侧 |

示例：

```text
左红右绿 → 走右侧
左绿右红 → 走左侧
左侧只有绿灯 → 走左侧
右侧只有绿灯 → 走右侧
左侧只有红灯 → 走右侧
右侧只有红灯 → 走左侧
```

“没有灯的一侧可以通过”只适用于岔路的“只放红灯”模式，不能套用到普通路段的红灯停、绿灯行任务。

### 6.2 程序判定

如果程序需要同时支持三种布置，可采用：

```text
同时发现红灯和绿灯 → 选择绿灯侧
只发现绿灯 → 选择绿灯侧
只发现红灯 → 选择另一侧
识别冲突或无法确定左右位置 → 停车并报告失败
```

如果比赛前已确定且不会临时改变灯光布置，也可以将本次布置方式设为配置参数，但不能把方向写死成永远向左或永远向右。

### 6.3 证据照片

照片必须：

- 用圆圈标出识别到的红灯或绿灯；
- 说明灯位于左路还是右路；
- 说明最终选择方向；
- 机器人随后实际进入正确道路。

示例：

```text
Team 10 detects a green light on the left way and chooses left
```

只放红灯时可使用：

```text
Team 10 detects a red light on the left way and chooses right
```

仅完成识别和拍照、但没有实际进入正确道路，不能视为完成任务。

---

## 7. 无拥堵岔路任务

道路出现左右两条岔路，其中一条存在机器人形成交通拥堵。机器人必须选择另一条未拥堵道路。

### 7.1 必须完成的动作

```text
发现岔路
→ 检测左右道路
→ 判断哪侧存在造成拥堵的机器人
→ 选择另一侧
→ 实际进入正确道路
```

无法可靠判断时，不得随机选路，应停车或返回失败状态。

### 7.2 证据照片

照片必须：

- 用矩形框标出造成拥堵的机器人；
- 写明拥堵位于左侧还是右侧；
- 写明最终选择的道路；
- 示例：

```text
Team 10 detects traffic jam on the left way and chooses right
```

正确完成该岔路选择按 15 分处理。

---

## 8. 障碍绕行任务

路线中会放置具有一定宽度和长度的障碍物。机器人必须：

```text
检测障碍
→ 保持安全距离并停车
→ 判断从左侧还是右侧绕行
→ 绕过障碍
→ 重新找到路线
→ 恢复巡线
```

不得撞上障碍，也不能绕过障碍后永久丢失路线。

### 8.1 证据照片

照片必须：

- 清楚显示障碍物；
- 写明检测到障碍；
- 写明选择的绕行方向；
- 示例：

```text
Team 10 detects an obstacle and chooses the left side
```

完整绕过障碍并返回路线得 15 分。

---

## 9. 断线恢复任务

道路中会存在断开的线路。机器人必须：

```text
巡线时发现持续断线
→ 自主搜索下一段正确路线
→ 找到并确认正确路线
→ 恢复正常巡线
```

该过程不能依赖人工移动、转向或遥控机器人。

### 9.1 证据照片

应在确认重新找到正确路线后保存：

- 显示重新找到的路线；
- 对路线进行必要标注；
- 写明已经找到正确路线；
- 示例：

```text
Team 10 finds the correct line to follow
```

成功恢复巡线得 10 分。

---

## 10. 证据照片统一规则

课件多次强调：任务得分依据保存的证据照片核定。

这不表示保存越多照片得分越高。正确做法是：

- 每个有效计分事件保存一张合格证据图；
- 相同事件不要连续保存大量重复图片；
- 未完成动作时不要提前生成“成功照片”；
- 照片必须来自该次实际运行；
- 照片必须包含规定的矩形框、圆圈和文字；
- 照片内容必须与机器人实际行为一致；
- `cv2.imwrite()` 返回成功后，才能把证据记为已保存；
- 不同尝试的照片不能互相覆盖。

建议由公共证据模块统一执行：

```text
接收当前画面
→ 接收任务类型、目标位置和任务结果
→ 绘制矩形/圆圈/文字
→ 检查是否已经保存过
→ 按统一文件名保存
→ 返回保存成功或失败
```

建议目录：

```text
evidence/
├─ attempt_01/
│  ├─ marker_1.jpg
│  ├─ marker_2.jpg
│  ├─ traffic_red_stop.jpg
│  ├─ traffic_green_go.jpg
│  ├─ fork_signal.jpg
│  ├─ fork_jam.jpg
│  ├─ obstacle.jpg
│  └─ line_recovery.jpg
├─ attempt_02/
└─ attempt_03/
```

---

## 11. 比赛尝试与人工干预

- 每组有三次尝试；
- 每次尝试都必须从起点重新开始；
- 每次人工干预扣 5 分；
- 每次尝试开始时应重置运行状态；
- 数字标识已完成集合应在新尝试开始时清空；
- 每次尝试使用独立证据目录；
- 必须录制完整运行过程。

课件没有明确说明三次尝试最终取最好一次、最后一次，还是允许组合证据。因此：

- 必须保留三次尝试各自完整的代码版本、录像和证据目录；
- 不得擅自把不同尝试的照片混合为同一次完整成绩；
- 最终计分方式以现场教师或助教说明为准。

---

## 12. 最终提交材料

课件要求准备：

1. 每名成员负责代码部分的截图；
2. 团队保存的各项任务证据照片；
3. 最终任务 Python 源代码；
4. 完整运行过程录像；
5. Word 文档，其中至少包含：
   - 个人代码截图；
   - 数字标识照片；
   - 普通红灯和绿灯照片；
   - 红绿灯岔路照片；
   - 无拥堵岔路照片；
   - 障碍照片；
   - 断线恢复照片。

课件中的考试和上传日期是 2025 年旧日期，不应直接作为当前项目截止时间。当前考试时间、提交截止时间和提交入口必须以最新通知为准。

---

## 13. 对 Codex/AI Agent 的硬性约束

### 13.1 必须遵守的规则

- 机器人必须自主完成任务；
- 数字 ID 只识别 1-5；
- 数字拍摄顺序不限，不要求从 1 开始；
- 每个数字在一次尝试中最多计分一次；
- 同屏多个有效数字标识时选择最近者；
- 标识足够大后才允许对准拍照；
- 数字标识必须在画面中心附近；
- 普通红灯停车与普通绿灯继续分别计分；
- 普通路段和岔路的红绿灯属于两个独立任务；
- 岔路总原则是走有绿灯的道路，或走没有红灯的道路；
- 无拥堵岔路必须避开存在交通拥堵的道路；
- 障碍绕行后必须返回路线；
- 断线后必须自主找到并恢复正确路线；
- 所有计分任务必须生成对应证据照片；
- 人工干预会扣分；
- 每次尝试必须从起点重新开始；
- 每名成员必须有被最终主程序调用的独立代码部分。

### 13.2 工程实现建议，不得伪装成课件原文

- 使用连续多帧确认减少误识别；
- 使用非阻塞状态机完成停车、等待、转向、绕行和恢复；
- 使用统一证据模块保存照片；
- 用标识框宽度近似数字标识的距离；
- 把阈值、速度、超时和岔路灯光模式做成可配置参数；
- 无法可靠识别时安全停车或返回失败，不随机行动；
- 分尝试保存照片、录像、参数和运行日志；
- 保存照片成功后再更新任务完成状态。

### 13.3 不得自行假定的事项

- 三次尝试最终采用哪一次成绩；
- 障碍物精确尺寸；
- 岔路精确角度和尺寸；
- 灯具的实际 HSV 范围、亮度和物理位置；
- 当前考试日期和提交截止时间；
- 现场对“人工干预”的具体边界。

这些事项必须通过现场观察、测试或教师/助教的最新说明确认。

---

## 14. 一次完整运行的推荐任务逻辑

以下顺序仅表示程序需要具备的通用处理能力，不代表赛道一定按该顺序布置：

```text
初始化本次尝试
→ 创建独立证据目录
→ 清空数字标识完成集合
→ 开始巡线
→ 按实际出现顺序处理数字标识、断线、障碍、岔路和普通红绿灯
→ 每完成一个计分事件立即保存对应证据
→ 任务完成或安全失败
→ 保存本次尝试的录像、参数、日志和结果清单
```

程序不能假设数字标识或其他任务一定按照固定顺序出现。
