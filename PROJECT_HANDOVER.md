# 项目负责人交接

状态日期：2026-09-11。本文只记录仓库能够证明的事实；期末评分细则、场地和道具没有可靠文件来源的部分均标为待确认。

## 1. 目标、仓库与版本

项目目标是在低速稳定巡线底座上，逐步接入数字标识 1～5、红绿灯、障碍绕行、长断线巡回、绿灯岔路、无拥堵岔路及截图证据，最后形成完整任务流程。具体顺序、得分、强制交付物和失败规则仍需从教师/赛项文件确认。

- 本地项目：`F:\robomaster\final_project`
- 远程：`https://github.com/ArcheSiegfried/robomaster-final-project`
- 原始第一版：`fa4d7e1c378c41d9ca42535cb0c7b8f09203a083`
- 原始标签：`v0.1-original-baseline`
- 协作基线：以 `v0.2-collaboration-baseline` 标签解析出的提交为准，使用 `git rev-parse v0.2-collaboration-baseline` 查看。

2026-09-11 实际查询时远程仓库为公开可见，与任务背景所称“私有仓库”不一致。没有在本轮擅自修改可见性；仓库所有者必须确认并按需要恢复私有。

## 2. 审查结论

### 今晚必须修复

- 原离线接管示例只用文字表示 `MotionOutput` 切换；现已用 `FakeChassis` 真正执行 `claim/send/hard_stop` 并增加回归断言。
- 文档需要统一为 `任务分支 → PR 到 integration → 联调 → main → 标签`，并给新手可直接执行的命令。
- 原 `TASKS.md` 缺少优先级、允许/禁止范围、实车要求、交回证据和降级方案；协作材料与 PR 模板需要补齐。
- 离线命令需要一个不会依赖 PowerShell 通配符展开的统一脚本。

### 可以作为后续任务

- 正式数字、灯、截图、障碍、长断线/岔路和总体任务状态机。
- 收集场地数据后调整 HSV、ROI、PD、速度及各种任务阈值。
- 根据实际整合压力决定是否增加少量任务协调代码；当前不建立插件或事件系统。

### 不需要修改

- `LatestFrameSource` 已真实产生 `FramePacket(image, sequence, captured_at)`，主循环真实消费同一包；未来检测器可共享该帧。
- `LineDetection`、`VisualDetection`、`MotionCommand`、`TaskStatus` 和 `TaskUpdate` 已能表达第一阶段接入需求，v0.1 字段无需扩张。
- `LineFollower.pause/reset_fault/resume` 已区分人工停止、故障复位和新鲜有效路线显式恢复，并清理历史。
- `MotionOutput` 是生产代码中唯一正常底盘出口；`main.py` 没有直接调用 chassis。
- 普通漏检最多容错 0.28 秒，持续丢线进入 `LINE_LOST` 锁停；没有搜索状态，不冒充长断线任务。
- 视频无新帧使用独立 `VIDEO_LOST`，不会被当作图像漏检继续运动；异常和退出有硬停车清理。

### 必须实车确认

- forward/lateral/yaw 的实际符号、底盘命令 timeout 和两种硬停车调用在当前固件上的效果。
- HSV、ROI、线宽筛选、相机位姿、PD/前馈、速度、短时容错和真实弯道稳定性。
- 视频/Wi-Fi 中断时零速命令是否能送达及 SDK 自身安全超时。
- 所有正式视觉模块的距离、光照、误检/漏检与动作精度。

## 3. 当前真实能力

已实现并离线验证：

- `camera_source.py` 的最新帧线程、递增序号、单调时间和相机异常传播。
- `line_detector.py` 的固定 ROI、可配 HSV、形态学、有效轮廓筛选与近远采样。
- `controller.py` 的低速 PD/方向项、速度/转向/变化率限制与恢复渐变。
- `runtime.py` 的普通短时漏检、持续丢线锁停、视频失效锁停、暂停/复位/显式恢复。
- `motion_output.py` 的唯一 owner、错误 owner 拒绝和硬停车。
- `examples/offline_takeover.py` 的正常巡线、任务接管、运动请求、完成、归还、新帧确认与恢复。

已经编码但未实车验证：基础巡线整体表现、相机/机器人生命周期、云台对齐、命令方向/超时、键盘操作及真实断网停车。

尚未实现：数字、灯、障碍、长断线、两类岔路、正式截图证据、模块协调和整场流程。14 项离线测试不能证明这些功能，更不能证明期末得分或实车稳定。

## 4. 架构和高冲突文件

```text
main.py
  ├─ LatestFrameSource -> FramePacket（唯一相机入口）
  ├─ LineFollower -> RuntimeDecision / MotionCommand
  └─ MotionOutput（唯一正常底盘出口）

未来任务：共享 FramePacket -> VisualDetection / TaskUpdate / MotionCommand
```

`models.py`、`camera_source.py`、`runtime.py`、`motion_output.py`、`main.py` 是高冲突公共文件，普通模块不得自行修改。接口详情以 `MODULE_GUIDE.md` 的 v0.1 为准。

短时漏检属于底座：`COASTING` 仅处理反光和少数帧漏检，0.28 秒后锁停。长断线属于未来外部任务：接管前暂停巡线，运动经 `MotionOutput`，步骤非阻塞且有硬超时；结束后停车、归还 owner、清历史、处理新帧并显式恢复。不得延长短时预算代替长断线。

## 5. 分支用途和权限边界

- `main`：只保存经过联调的稳定状态；不作为日常业务开发分支。
- `integration`：模块 PR 的唯一常规目标；集中做离线和实车组合验证。
- `feat/...`、`fix/...`：普通成员从最新 `integration` 创建，一包一分支。
- `chore/...`：文档、工具或仓库维护。
- 稳定标签：为已验证的 `main` 留回退点，不移动已有标签。

总负责人决定范围、优先级、人员、降级、实车时段和最终交付；整合负责人审查接口/安全、维护 `integration`、协调高冲突文件并提出合入 `main` 的建议。两种角色可以由同一人承担，但普通模块开发者不因此获得直接推送公共分支或改公共接口的权限。

## 6. 优先级与时间建议

详细工作包见 `TASKS.md`。建议依赖：WP1 基础安全先行；WP2/WP3/WP4 可在样本到位后并行；WP5/WP6 依赖场地和可靠底座；WP7 从第一天就用假模块维护集成，但正式完成度取决于前六包。

接下来 24 小时：

- 确认仓库可见性、邀请、分支保护和任务领取方式。
- 获取教师规则、场地图/尺寸、道具照片与现有录像。
- 在负责人监督下完成 WP1 的架空符号/停车检查；没有安全条件就只做离线。
- 让候选模块先交一组正负样本与最小测试，不急于改主流程。

接下来 3 天：

- 固定一套可复现的低速配置；优先完成规则最明确的数字/灯检测。
- 接通截图证据的最小路径；用假任务验证 `integration` 控制权切换。
- 只有几何和安全边界明确后才推进障碍、长断线和岔路。

提交前：

- 冻结提交和配置，做完整离线回归，再按首次实车清单逐级测试。
- 将稳定 `integration` 合入 `main` 并打不可移动标签。
- 汇总 `DELIVERABLES.md`，明确失败案例、已知问题和未验证事项。

可降级或放弃：通用插件平台、大范围智能搜索、复杂规划/自动调参、规则未知的通用识别、无可靠距离的自动绕障、没有确定判据的拥堵岔路。最低目标是可重复低速巡线、可靠停车、最可靠的一到两个任务和可追溯证据。

## 7. 合并审查

- 分支是否来自近期 `integration`，改动是否只属于一个工作包。
- 输入是否来自共享 `FramePacket`，输出是否为 v0.1 公共类型。
- 是否没有第二相机、SDK 直调、阻塞循环、无限动作或绕过 `MotionOutput`。
- 无目标、错误目标、超时、失败、完成和恢复是否有测试。
- 公共文件变化是否已协调、解释接口缺口并补集成测试。
- PR 是否给出准确命令、实际结果、数据来源和未验证项。
- 是否没有密钥、代理、绝对本机路径、缓存、日志或大批录像。

## 8. 首次实车验证清单

1. 记录当前 commit SHA、配置文件、机器人/固件、相机安装和场地条件。
2. 指定一人操作、一人准备急停、一人记录；清空安全区。
3. 不运行任何任务模块，先架空车轮验证前进、横移、yaw 符号和速度上限。
4. 逐一测试 SPACE 暂停、R 复位、Q/ESC/Ctrl+C、异常退出和窗口关闭。
5. 架空模拟持续丢线和视频中断，确认不会自动恢复。
6. 地面从最低速度做短直线，再做偏线、弯道和短遮挡；每次只改少量参数。
7. 断网和障碍测试必须留物理安全区；任何停车不可靠立即终止。
8. 保存对应 SHA、参数、成功/失败结果和视频索引，不能只记“通过”。

## 9. 仓库所有者必须处理

- 确认为什么远程当前公开，并决定是否恢复私有。
- 邀请协作者并设置最小权限；不虚构人员或贡献。
- 为 `main` 和 `integration` 设置禁止强推、要求 PR；是否要求审查次数按团队能力决定。
- 确认学校的评分、场地、道具、提交物和截止时间。
- 决定任务取舍、整合负责人、实车安全安排和最终发布标签。
