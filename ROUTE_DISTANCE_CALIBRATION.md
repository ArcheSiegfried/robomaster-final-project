# 断口前向测距标定（本地试验）

本版只估计沿旧线路前进方向，到合适转向位置还差多少米；不是二维定位或底盘里程计。标定文件 `route_distance_calibration.json` 只保存在本机，已被 Git 忽略。没有该文件时，`route_only_main.py` 保持原先的固定 5 cm 后退化流程，并在路线日志里注明 fallback。

## 数据怎么采

先确定右角断线的预期转向位置（建议用旧线路中心轴与新线路中心轴的延长线交点），在地上作记号。相机分辨率、安装位置、地面、云台搜索俯仰角必须与测试一致；默认搜索角为 `-12°`。把车**人工**沿旧线路中心轴放在距该转向位置约 15、20、30、40 cm 等至少三个已知位置，车身方向保持不变。这里距离指车身旋转中心到转向位置在旧线路方向上的距离，而不是两段胶带端点的直线距离。

各位置保存一张云台已稳定在搜索角度的静态图像。标定脚本**只读取图片，不连接机器人，也不驱动车辆**。如果现场只能得到 CSV 中的断口端点纵坐标，可将 `candidate_lower_y / 图像高度` 作为 `ENDPOINT_Y_RATIO` 手工输入；必须确认框的是同一个新线路断口。不要把未测量的示例数字当作标定数据。

```powershell
.\.venv\Scripts\python.exe route_distance_calibrate.py --image 0.15 photos\15cm.png --image 0.25 photos\25cm.png --image 0.40 photos\40cm.png
```

或者手工输入实际测得的比例（下面数值**只是命令格式示例，不是真实标定**）：

```powershell
.\.venv\Scripts\python.exe route_distance_calibrate.py --point 0.15 0.95 --point 0.25 0.75 --point 0.40 0.55
```

脚本要求像素比例随车接近断口而升高，距离相应下降；至少三点，距离限于 0.10–0.45 m。请打开生成的 JSON 检查每个值。图片若有多条蓝线，自动选出的断口可能错误，应改用确认过的手工比例或裁出仅包含新线的静态图片。

## `route_only_main.py` 如何使用

把 JSON 放在项目根目录，随后照常运行 `route_only_main.py`。它只在相同搜索角度下加载表；角度不符或文件损坏会退回旧流程。新断口通过原有几何与连续帧门槛后，程序逐帧根据断口纵坐标更新剩余前向距离，分段请求 `0.15/0.12/0.08 m/s`；距离进入约 1.5 cm 时停车转向。断口离开标定视野后，最多允许 12 cm 的有界估算续行；剩余距离更大、读数超出标定范围或 5 秒内到不了转向点则故障停车。所有运动仍由主流程的唯一出口发送。

CSV/终端信息应显示 `measured turn-point approach; remaining ...`，到点显示 `measured turn point reached`。若显示 `fallback`，说明标定没有生效。测距阶段最多 5 秒；纯旧流程仍是 3 秒跨越上限。首次实车测试应在空旷地、有人准备急停条件下低风险验证；上述数值仍未实车验证。

当前协调器对整个任务另有 20 秒硬上限，不能在 `route.py` 私自延长。如果前面搜索/转向占用过久，最后低头居中仍可能没有完整 3 秒；这需要负责人与公共协调层共同决定是否增加总上限，不能把它当作标定完成的证据。
