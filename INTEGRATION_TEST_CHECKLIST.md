# 整合版实车联调清单（尚未执行）

本分支只经过语法、安全导入、静态调用链和少量定向离线检查；下面任何一项都**不能**写成已经实车通过。先记录本次 commit、参数、场地、机器人状态和尝试编号。由现场负责人安排人员清空安全区、监护急停，再人工运行 `main.py`。

1. 架空车轮：核对前进/横移/yaw 符号、唯一底盘出口、云台 FREE/CHASSIS_LEAD 切换、SPACE/R/Q/ESC、断网与退出停车。若方向错误或停车不可靠，立即停止后续测试。
2. 基础巡线：短直线、急弯、短时漏检、持续丢线、视频失效；记录每次是否自主恢复或按规则锁停。
3. 单任务逐项：数字 1～5 中至少两个非顺序 ID（拍照后确实再次运动、同 ID 不重触发）；普通红停与明确绿放；单侧绿灯岔路左右各一次；无拥堵岔路；障碍绕行后回线；直角断线和一个非直角断线。每项都保留成功、失败和未触发记录。
4. 核对 `captures/run_*` 中的正式得分图：完整画面、`Team 10`、数字矩形和 ID、普通红/绿圆圈各一张、岔路灯圆圈与选边、拥堵机器人矩形与选边、障碍矩形与绕行方向、真正重获新线后的断线框。失败/超时不得有成功照片；每次运行目录独立。
5. 连续场景：至少数字→红绿灯→障碍→岔路→断线的实际出现顺序组合，逐次核对任务 owner 归还、云台归位、巡线新鲜帧恢复、不重复接管。现场用运行日志和完整录像标记每次异常时间。
6. 若任一证据图写入失败、灯光冲突、选路不确定、任务超时或视频丢失，确认机器人停车且需要明确的人为恢复；记录是算法失败、场地不匹配还是集成错误。

现场结论按“提交 SHA → 参数 → 场地/道具 → 录像/日志/照片路径 → 结果”填写，不能把离线通过当成整场成功。最终评分与提交材料按项目根目录的 `RoboMaster_期末项目规则书.md` 和教师最新通知核对。
